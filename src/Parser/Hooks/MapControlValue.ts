export function TickUpdate(raw: Buffer): {} {
    const data = new DataView(raw.buffer, raw.byteOffset, raw.byteLength);
    
    /*
    var E = 1,
    S = XP >= 1 ? 2 : 1;
    let val: number = data.getFloat32(E);
    */
    
    return {}
} 